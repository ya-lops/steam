---
const { class: className, ...rest } = Astro.props;
---

<div class:list={['container', className]} {...rest}>
  <slot />
</div>

<style>
  .container {
    width: min(100%, 1388px);
    margin-inline: auto;
  }
</style>